<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainActivity">

    <!--    <ImageView-->
    <!--            android:id="@+id/pdfImage"-->
    <!--            android:layout_width="match_parent"-->
    <!--            android:layout_height="match_parent"-->
    <!--            android:background="@android:color/white"-->
    <!--            android:contentDescription="@null"-->
    <!--            android:scaleType="fitCenter" />-->
    <Button
            android:text="@string/previous"
            android:layout_width="202dp"
            android:layout_height="60dp" android:id="@+id/previous"
            app:layout_constraintStart_toStartOf="@+id/pdfLayout"
            app:layout_constraintBottom_toBottomOf="@+id/pdfLayout"/>
    <Button
            android:text="@string/next"
            android:layout_width="205dp"
            android:layout_height="60dp" android:id="@+id/next"
            app:layout_constraintBottom_toBottomOf="@+id/pdfLayout" app:layout_constraintEnd_toEndOf="@+id/pdfLayout"/>
    <LinearLayout
            android:id="@+id/pdfLayout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            tools:layout_editor_absoluteY="0dp" tools:layout_editor_absoluteX="0dp">
    </LinearLayout>


</androidx.constraintlayout.widget.ConstraintLayout>